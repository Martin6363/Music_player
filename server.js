// const express = require('express');
// const app = express();
// const path = require('path');

// const PORT = process.env.PORT || 3000; // You can change the port number if needed

// // Serve static files (HTML, CSS, JavaScript, and audio files) from the 'public' and 'tracks' folders
// app.use(express.static(path.join(__dirname, 'public')));
// app.use('/tracks', express.static(path.join(__dirname, 'tracks')));

// app.get('/', (req, res) => {
//   // Send your main HTML file
//   res.sendFile(path.join(__dirname, 'public', 'index.html'));
// });

// app.listen(PORT, () => {
//   console.log(`Server is running on http://localhost:${PORT}`);
// });
